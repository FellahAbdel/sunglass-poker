/**
 *  This file should contain all type of rules and the specifics of each rules.
 * 
 * This will allow future implementation to contain differents kind of game instead of the Texas holdem.
 * 
 */

module.exports = texasHoldem = {
    max:10,
    min:3,
}